import React from 'react';

// This component represents the header of the Hangman game.
const Header = () => {
  return (
    <header>
      {/* Display the title of the game */}
      <h1>Hangman Game</h1>
    </header>
  );
};

export default Header;
